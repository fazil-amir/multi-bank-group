# Local dev: point the client at the API (optional; dev defaults to http://localhost:4000)
# VITE_API_BASE_URL=http://localhost:4000

# Production (e.g. Render): set PORT; NODE_ENV=production is set automatically.
# PORT=4000
